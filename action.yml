name: 'Hello world Sahil Action'
description: 'Greet someone and record the time'

branding:
  icon: 'activity'
  color: 'green'

inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: 'World'

outputs:
  time:
    description: 'The time we greeted you'

runs:
  using: 'node20'
  main: 'dist/index.js'